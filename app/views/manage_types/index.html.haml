%h3= t(".header")
%p#notice= notice
%table.table
  %tr
    %th= t(".header_table")
    %th
    %th
    %th

  - @manage_types.each do |manage_type|
    %tr
      %td= 	manage_type.name
      %td= link_to t("links.destroy"), manage_type_path(manage_type), :method => :delete, :data => { :confirm => t('links.confirm') }

%br

= link_to t(".link_new"), new_manage_type_path
